node_modules/
dist/
/data/
.env
.env.local
.superpowers/
*.log
.DS_Store

# 内部设计/计划文档（含私人引用，不发布）
docs/

# 本机实际用的 compose（生产配置，因机器而异）；仓库用 docker-compose.example.yml
docker-compose.yml

# Claude Code worktrees（重构等隔离工作区）
.claude/worktrees/
